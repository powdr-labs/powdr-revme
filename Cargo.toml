[package]
name = "powdr_revm"
version = "0.1.0"
edition = "2021"

[dependencies]
critical-section = "1.1.2"
revm = { git = "https://github.com/powdr-labs/revm.git", branch = "no-std", default-features = false }
runtime = { git = "https://github.com/powdr-labs/powdr.git", branch = "main" }
